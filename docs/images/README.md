# Runtime screenshots

These images were captured from the running application in a separate demo database on 2026-09-13. The demo uses the built-in sample securities and public market data loaded through the application's sync endpoints. The UI language is Chinese.

- `dashboard.jpg`: the homepage market overview, focus security, benchmark cards and search entry.
- `stock-detail.jpg`: the price-history section of the Ping An Bank demo detail page, including candlesticks, volume and price rows.
- `ai-settings.png`: the web AI configuration form, captured separately on 2026-09-13 against an isolated backend and local mock provider. The displayed model and address are test fixtures; the successful connection message is a fixed-message local test, not external AI availability evidence. No credential is displayed.

Images are direct browser captures without compositing or generated UI. No personal holdings or real user watchlist is included. Prices and dates are point-in-time snapshots; no fresh AI generation is represented.
